# Deploy uses the Dockerfile + env.yml (conda-forge), not this file. Kept for
# reference / a native-buildpack attempt.
#
# --- original science stack ---
Flask
numpy
matplotlib
rasterio
scikit-learn
gunicorn

# --- live NBM ingest ---
requests
pyproj

# --- GRIB2 decoding for NBM (needs the ecCodes C library -> use the Dockerfile) ---
eccodes
cfgrib
xarray

# --- dev only: building data/roads_segments.json via `python app.py --bake-roads`.
#     NOT needed at runtime (the server reads the prebaked JSON). ---
# geopandas
# shapely
